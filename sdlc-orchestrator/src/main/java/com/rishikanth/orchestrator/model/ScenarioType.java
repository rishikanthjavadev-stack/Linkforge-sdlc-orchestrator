package com.rishikanth.orchestrator.model;

/**
 * The three required demonstration scenarios. Each maps to a different
 * graph shape / node behavior via GraphFactory, not just different input text.
 */
public enum ScenarioType {
    GREENFIELD,
    BROWNFIELD,
    AMBIGUOUS
}
